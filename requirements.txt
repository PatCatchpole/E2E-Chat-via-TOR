# Relay (server/) and client (client/).
#
# Flask-SocketIO must be >=5.4: 5.3.x sets `ctx.session`, which Flask 3.1
# removed, so the relay fails on every socket event with
# "AttributeError: can't set attribute".
Flask==3.1.3
Flask-SocketIO==5.6.1
python-socketio==5.16.4
python-engineio==4.13.5

# Without simple-websocket the transport silently degrades to HTTP polling.
simple-websocket==1.1.0

# Client
PyNaCl==1.6.2
requests[socks]==2.32.3
prompt_toolkit==3.0.52

# bcrypt is deliberately absent: password hashing now happens in the backend.
# The client derives a PBKDF2 verifier with hashlib from the standard library.

# Desktop window (client/desktop). Optional: without it the launcher falls
# back to the terminal interface. Not installed on Linux, where it needs
# system GTK/WebKit packages rather than wheels.
pywebview==6.2.1; sys_platform == "darwin" or sys_platform == "win32"
